
export const SiteInfo = ({ onClose }) => {
    const handleSiteInfo = () => {
      console.log('사이트 설명(미구현)');
    };
  
    return (
      <button className="npc-button" onClick={handleSiteInfo}>
        사이트 설명(미구현)
      </button>
    );
  };