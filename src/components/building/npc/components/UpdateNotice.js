export const UpdateNotice = ({ onClose }) => {
    const handleUpdateNotice = () => {
      console.log('업데이트 공지(미구현)');
    };
  
    return (
      <button className="npc-button" onClick={handleUpdateNotice}>
        업데이트 공지(미구현)
      </button>
    );
  };