export const AIChat = ({ onClose }) => {
    const handleAIChat = () => {
      console.log('대화하기(미구현)');
    };
  
    return (
      <button className="npc-button" onClick={handleAIChat}>
        AI 대화하기(미구현)
      </button>
    );
  };